tsconfig.default.json

tsconfig.prod.esm.json
tsconfig.prod.cjs.json
tsconfig.prod.types.json

tsconfig.dev.esm.json
tsconfig.dev.cjs.json
tsconfig.dev.types.json

## todo

    // "build": "tsc --project tsconfig.default.json",
    // "build:watch": "tsc --project tsconfig.default.json --watch",
    // "start": "grunt",
    // "test": "vitest run --config vitest.config.ci.ts",
    // "test:vitest": "vitest run --config vitest.config.dev.ts",
    // "test:vitest:ci": "vitest run --config vitest.config.ci.ts",
    // "test:vitest:ui": "vitest run --ui --config vitest.config.dev.ts",
    // "test:vitest:watch": "vitest --config vitest.config.dev.ts",
    // "test:vitest:visual": "VEXFLOW_VISUAL_REGRESSION=1 vitest run --config vitest.config.dev.ts",
    // "test:vitest:visual:ci": "VEXFLOW_VISUAL_REGRESSION=1 vitest run --config vitest.config.ci.ts",
    // "test:vitest:visual:watch": "VEXFLOW_VISUAL_REGRESSION=1 vitest --config vitest.config.dev.ts",
    // "test:vitest:clean": "rm -rf tests/__screenshots__",
    // "clean:build": "grunt clean:build",
    // "webpack:watchDebug": "grunt webpack:watchDebug"
